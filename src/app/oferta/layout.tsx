/**
 * Layout limpio para la landing de /oferta.
 * No hereda el Navbar de Prospector ni el wrapper <main> del root layout.
 */
export default function OfertaLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
